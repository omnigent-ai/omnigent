# MCP startup UI demo

Screenshots from the Playwright regression for a resumed session with stale MCP startup state. Before: the MCP band remains after live assistant text. After: the band is hidden while the working indicator remains active, including after a late MCP update. The plan is a same-stream event barrier in the regression test. These images contain only synthetic test data.
