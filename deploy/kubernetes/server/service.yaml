---
apiVersion: v1
kind: Service
metadata:
  name: omnigent
  namespace: omnigent
  labels:
    app: omnigent
spec:
  selector:
    app: omnigent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
