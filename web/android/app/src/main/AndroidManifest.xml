<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Runtime-requested on API 33+; see notification flow. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Runtime-requested when the web UI first asks for the mic (voice input). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!--
      allowBackup=false: the WebView cookie store holds the authenticated
      __Host-ap_session cookie, so cloud Auto Backup and `adb backup` would
      otherwise copy a live session (and the saved server list) off-device. A
      server URL is trivially re-entered; a session is not worth exfiltrating.
    -->
    <application
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Omnigent">

        <!-- Lets an EMM/MDM preconfigure server URLs; read by ManagedConfig. -->
        <meta-data
            android:name="android.content.APP_RESTRICTIONS"
            android:resource="@xml/app_restrictions" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode|smallestScreenSize|screenLayout|density"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConnectActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- omnigent://<host>/c/<id> deep links. Custom schemes are unverified —
             any co-installed app can also claim
             this one — so DeepLink.parse (in MainActivity) treats the URI as
             untrusted. This trampoline only forwards: task-fronting can swallow
             a VIEW intent aimed straight at a non-top MainActivity (verified
             on-device), so the filter lives here instead.
             taskAffinity="": a task whose base intent/root activity matches an
             incoming VIEW+NEW_TASK launch gets fronted WITHOUT delivery (the
             same drop, one level up — hit this even with the filter moved here,
             once this activity had rooted a task shared with MainActivity's
             default affinity). An empty affinity gives every launch its own
             disposable task, torn down the instant this activity finishes, so
             no later launch can ever match it and get silently fronted. -->
        <activity
            android:name=".DeepLinkActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Omnigent.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="omnigent" />
            </intent-filter>
        </activity>
    </application>

</manifest>
