# Omnigent IntelliJ/PyCharm plugin — build pins.

# Single source-of-truth plugin version (also reflected in plugin.xml at build
# time and in the artifact filename omnigent-intellij-<pluginVersion>.zip).
pluginVersion=0.1.0

# Target IntelliJ Platform. Default IDEA Community 2024.1.
#   IC = IntelliJ IDEA Community
#   IU = IntelliJ IDEA Ultimate
#   PC = PyCharm Community
#   PY = PyCharm Professional
# Switch via -PplatformType=PC (etc.) to build/verify the other IDEs; the plugin
# targets IDEA + PyCharm (Community + Professional) — see plugin.xml depends.
platformType=IC

# 2024.1 is the first platform line whose bundled JBR is JCEF-capable, which
# this plugin's tool window requires.
platformVersion=2024.1

# sinceBuild pinned to the same 2024.1 line; untilBuild is left open (see
# build.gradle.kts — must be set to `null` explicitly or the Gradle plugin
# auto-derives a blocking upper bound).
sinceBuild=241

# Gradle / JVM.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true

# Prevents Gradle from auto-bundling kotlin-stdlib alongside the platform's own
# bundled copy (flagged by the plugin verifier otherwise).
kotlin.stdlib.default.dependency=false
